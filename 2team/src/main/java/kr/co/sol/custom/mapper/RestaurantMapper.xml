<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.sol.custom.dao.RestaurantDAO">
    
    <select id="getRestaurants" parameterType="tdto" resultType="tdto">
    	select t.no, t.c_no , c.name c_name, t.name , address1 , address2 , tel , hour 
    	from restaurant t join category c 
    	on t.c_no = c.no 
    	
    	<choose>
    		<when test="no != 0">
    			where t.no = #{no}
    		</when>
    	</choose>
    </select>
  	
  	<select id="getMenus" parameterType="rdto" resultType="ndto">
  		select m.food_no , c_no, name, price 
  		from food f join r_menu m 
  		on f.no = m.food_no 
  		where res_no = #{no}
  	</select>
  	
  	
  	<insert id="likeRestaurant" parameterType="int">
  		<selectKey keyProperty="no" resultType="int" order="BEFORE">
	       SELECT NVL(MAX(no),0)+1 no FROM favorites
      	</selectKey>
      	
      	insert into favorites(no,res_no,mem_no)
      	values(#{no},
      		   #{res_no},
      		   #{mem_no})
  	</insert>
</mapper>