<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.sol.store.dao.StoreDAO">

<select id="getStore" resultType="sdto">
select no, c_no, name, address, phone, time from restaurant
</select>
<select id="getStoreList" parameterType="String" resultType="sdto">
select no, c_no, name, address, phone, time from restaurant
where ${searchOption} like '%'||#{keyword}||'%'
</select>
</mapper>