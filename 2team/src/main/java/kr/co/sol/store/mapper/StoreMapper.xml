<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.sol.store.dao.StoreDAO">
<!-- 레스토랑 테이블의 모든 데이터를 가져오기 -->
<select id="getStoreList" resultType="sdto">
	select
		no, c_no, name, address, phone, time
	from
		restaurant
</select>
<select id="getStore" parameterType="String" resultType="sdto">
select
		no, c_no, name, address, phone, time
	from
		restaurant
where ${searchOption} like '%'||#{keyword}||'%'
</select>
</mapper>

